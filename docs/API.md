# Host API

To be moved from SPLA `tools/spla-diagram/docs/API.md`.
Host takes two explicit roots: `--workspace` (models) and `--source-root` (code for `codeRef`).
The editor bundle is served from the host installation, not from the workspace.
