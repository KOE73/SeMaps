{
  "name": "@semaps/extract-typescript",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "semaps-extract-typescript: prints EXTRACTOR.md facts for a TypeScript source tree.",
  "bin": {
    "semaps-extract-typescript": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pretest": "npm run build",
    "test": "node --test dist/test/*.test.js",
    "self": "npm run build --silent && node dist/cli.js --root ../.. --include editor/src"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "ajv": "^8.17.1"
  }
}
